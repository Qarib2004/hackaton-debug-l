const BASE_URL = "https://mica-grape-scabiosa.glitch.me/";

const endpoints = {
  events: "/events",
  tickets: "/tickets",
  users: "/users",
  venues: "/venues"
};

export { endpoints };
export default BASE_URL;
