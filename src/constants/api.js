const BASE_URL = "https://debug-legends-api.glitch.me";

const endpoints = {
  events: "/events",
  tickets: "/tickets",
  users: "/users",
  venues: "/venues"
};

export { endpoints };
export default BASE_URL;
